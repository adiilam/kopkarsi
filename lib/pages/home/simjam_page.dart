import 'package:flutter/material.dart';

class SimjamPage extends StatelessWidget {
  const SimjamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Simjam Page'),
    );
  }
}
